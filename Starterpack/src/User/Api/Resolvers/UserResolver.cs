using Starterpack.User.Domain.Models;
using Starterpack.User.Domain.Services;

namespace Starterpack.User.Api.Resolvers
{
    class UserResolver
    {
        // This is just a test
        // public async Task<IEnumerable<UserModel>>? GetAllUsers([Parent] UserModel user, [Service] IUserService userServie)
        // {
        //     return await userServie.GetUsersAsync(new CancellationToken());
        // }
    }
}